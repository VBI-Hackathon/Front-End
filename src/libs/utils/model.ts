export interface ProductInfo {
  userName: string;
  rd: string;
  address: string;
  owner: string;
  productName: string;
  datetime: number;
}

export interface IAccount {
  address: string;
  label: string;
}
