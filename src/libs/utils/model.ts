export interface ProductInfo {
  userName: string;
  rd: string;
  address: string;
  owner: string;
  productName: string;
  datetime: number;
}

export interface IAccount {
  address: string;
  label: string;
}

export interface IAccountInfo {
  userName: string;
  address: string;
  owner: string;
  productName: string;
  quantity: number;
  note: string;
  datetime: number;
}
